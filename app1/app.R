library(shiny)
ui <- fluidPage()
server <- function(input, output) {}

# IMPORTANTE --------------------------------------------------------------

# Esta debe ser la última línea

shinyApp(ui = ui, server = server)
